name: Daily Org Heartbeat

on:
  schedule:
    # Runs at 8:00 UTC every day
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  login-and-ping:
    runs-on: ubuntu-latest
    steps:
      - name: Install Salesforce CLI
        run: npm install @salesforce/cli --global

      - name: Authenticate via JWT
        env:
          SFDX_JWT_KEY: ${{ secrets.SFDX_JWT_KEY }}
        run: |
          echo "$SFDX_JWT_KEY" > portfolio.key
          sf org login jwt --client-id ${{ secrets.SFDX_CLIENT_ID }} --jwt-key-file portfolio.key --username ${{ secrets.SFDX_USERNAME }} --alias portfolio --instance-url https://login.salesforce.com

      - name: Execute Heartbeat Query
        run: sf data query --query "SELECT Id FROM User LIMIT 1" --target-org portfolio
